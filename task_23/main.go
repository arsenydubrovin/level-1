// Удалить i-ый элемент из слайса.

package main

import (
	"fmt"
)

func main() {
	a := []int{1, 2, 3, 4, 5, 6, 7}
	i := 3

	fmt.Printf("Слайс %v\n", a)
	fmt.Printf("Удаление элемента [%d]\n", a[i])

	// 1. Oператор ...
	a = append(a[:i], a[i+1:]...)

	// 2. copy()
	// a = a[:i+copy(a[i:], a[i+1:])]

	// 3. Без сохранения порядка, копирование из конца (самый быстрый и незатратный)
	// a[i] = a[len(a)-1]
	// a = a[:len(a)-1]

	fmt.Printf("После удаления: %v\n", a)
}
